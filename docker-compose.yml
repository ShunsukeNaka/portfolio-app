version: "3.9"

services:
  frontend:
    build: ./frontend
    container_name: frontend
    ports:
      - "80:80"
    environment:
      - NEXT_PUBLIC_API_URL=http://api:8080
    env_file:
      - .env
    depends_on:
      - api

  api:
    build: 
      context: ./api
    container_name: api
    ports:
      - "8080:8080"
    env_file:
      - .env
      